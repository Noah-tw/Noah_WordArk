/* lang_rules_he.js — Hebrew: no rules registered yet. Hebrew verb morphology (root+binyan
   templates) doesn't fit the suffix/stem pattern used elsewhere — that stays in forms[] /
   word_forms.js per word. A future addition here could cover the closed set of prefix
   particles (ו/ל/ב/כ/מ/ה) that fuse onto the next word, mirroring French elision but in
   the other direction. Until then this stays empty. */
LangRules.register('hebrew', {});
